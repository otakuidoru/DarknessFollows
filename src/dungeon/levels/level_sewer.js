/**
 * 
 */
var SewerLevel = Level.extend({
    /**
     * @param num
     */
    ctor:function(num) {
        //////////////////////////////
        // 1. super init first
        this._super(num);

        return true;
    }
});
