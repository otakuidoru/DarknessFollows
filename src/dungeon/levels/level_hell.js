/**
 *
 */
var HellLevel = Level.extend({
    numCells:0,

    /**
     * @param num
     */
    ctor:function(num) {
        //////////////////////////////
        // 1. super init first
        this._super(num);

        return true;
    }
});
